var name = "Another Console Message?!"; // This is a global variable, no?

document.write('This should be a headline!');

console.log(name);

const person = {firstName: 'Adan', lastName: 'Rubio', age:21, eyeColor: 'Unknown'};

document.getElementById("info").innerHTML =
person.firstName + lastName + 'is' + person.age + 'y.o';